[build]
  command = "npm run build"
  publish = "dist"

# Ensure client-side routing (React Router) doesn't 404 on refresh/deep links.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
